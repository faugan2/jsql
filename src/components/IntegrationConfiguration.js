import "../styles/integration_configuration.scss";
const IntegrationConfiguration=({app_key})=>{
    return(
        <div className="integration_configuration">
            APP_KEY={app_key}
        </div>
    );
}

export default IntegrationConfiguration;